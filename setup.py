import setuptools

setuptools.setup(
    name="dkpy",
    version="1.2",
    packages=["dkpy"],
    include_package_data=True,
    description="Super, extra library",
    install_requires=["pyserial"])
